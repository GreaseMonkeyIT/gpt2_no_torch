# Python / runtime — regenerated, or too large for git
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.obsidian/

# datasets — regenerate with `python run.py prep-shakespeare` / `prep-owt`
data/**/*.bin
data/**/*.pkl
data/**/input.txt
data/hf_cache/
data/owt_prep.progress.log

# training outputs — checkpoints + snapshots (figures are kept in plots/)
out/
*.npz

# non-submission material (personal notes, internal logs, scratch)
_local/

# internal planning/study docs — submission ships only README, DERIVATIONS, JOURNAL
CODEMAP.md
ROADMAP.md
SPRINT.md

# scratch demo notebook (not submitted)
try_it.ipynb
